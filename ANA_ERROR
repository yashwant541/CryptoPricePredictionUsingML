SELECT 
    *, 
    CASE 
        WHEN LINE_DESCR IS NULL OR TRIM(LINE_DESCR) = '' 
        THEN 'No Description'
        ELSE 'Length of Description is ' || LENGTH(LINE_DESCR)
    END AS Description_Category
FROM data
