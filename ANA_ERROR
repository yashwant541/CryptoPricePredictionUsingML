SELECT 
    d.ID, 
    d.line, 
    d.description, 
    COLLECT_SET(k.keyword) AS matched_keywords
FROM data_table d
LEFT JOIN keywords_table k 
    ON LOWER(d.description) LIKE CONCAT('%', LOWER(k.keyword), '%')
GROUP BY d.ID, d.line, d.description;
