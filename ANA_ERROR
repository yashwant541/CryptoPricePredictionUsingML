WITH RankedDescriptions AS (
    SELECT 
        ID, 
        description,
        LENGTH(description) AS desc_length,
        ROW_NUMBER() OVER (PARTITION BY ID ORDER BY LENGTH(description) DESC) AS rn
    FROM your_table
)
SELECT ID, description
FROM RankedDescriptions
WHERE rn = 1;
