import pandas as pd
import dataiku
import tempfile
import os
import shutil

# ------------------------------------------------------
# 🔧 Configuration
# ------------------------------------------------------
input_folder = dataiku.Folder("XXXXXX")   # 📥 Folder containing input CSV files
output_folder = dataiku.Folder("YYYYYY")  # 📤 Folder to save filtered CSV outputs

# ------------------------------------------------------
# 🧠 Helper: Keep only first + change rows
# ------------------------------------------------------
def keep_change_rows(df):
    df = df.copy()
    df = df.sort_values(by="Date")
    df = df.reset_index(drop=True)

    # Compare each row with the previous row across all tenor columns (excluding Date)
    change_mask = (df.iloc[:, 1:] != df.iloc[:, 1:].shift()).any(axis=1)

    # Always keep the first row + rows where any tenor MVT value changes
    keep_rows = change_mask | (df.index == 0)
    return df.loc[keep_rows].reset_index(drop=True)

# ------------------------------------------------------
# 🚀 Process each CSV file
# ------------------------------------------------------
for file_path in input_folder.list_paths_in_partition():
    filename = os.path.basename(file_path)

    if not filename.lower().endswith(".csv"):
        continue

    print(f"📄 Processing file: {filename}")

    # --------------------------------------------------
    # 📥 Read file from Dataiku stream
    # --------------------------------------------------
    tmp_input = tempfile.NamedTemporaryFile(delete=False)
    with input_folder.get_download_stream(filename) as stream:
        shutil.copyfileobj(stream, tmp_input)
    tmp_input.close()

    # Read CSV (UTF-8)
    df = pd.read_csv(tmp_input.name, dtype=str, encoding="utf-8")
    os.unlink(tmp_input.name)  # Clean up

    # --------------------------------------------------
    # 🧹 Prepare data
    # --------------------------------------------------
    if "Date" not in df.columns:
        print(f"⚠️ Skipping {filename} — 'Date' column missing.")
        continue

    # Convert Date column to datetime and sort
    df["Date"] = pd.to_datetime(df["Date"], errors="coerce")
    df = df.dropna(subset=["Date"])
    df = df.sort_values(by="Date").reset_index(drop=True)

    # Convert numeric columns (skip Date)
    for col in df.columns[1:]:
        df[col] = pd.to_numeric(df[col], errors="coerce")

    # --------------------------------------------------
    # 🧩 Apply change-detection logic
    # --------------------------------------------------
    filtered_df = keep_change_rows(df)

    # --------------------------------------------------
    # 💾 Save filtered CSV (UTF-8)
    # --------------------------------------------------
    output_filename = os.path.splitext(filename)[0] + "_Filtered.csv"
    tmp_dir = tempfile.mkdtemp()
    tmp_path = os.path.join(tmp_dir, output_filename)

    filtered_df.to_csv(tmp_path, index=False, encoding="utf-8")

    with open(tmp_path, "rb") as f:
        output_folder.upload_stream(output_filename, f)

    print(f"✅ Saved filtered output: {output_filename}")

print("🎯 Completed filtering — only date-change rows kept and saved as UTF-8 CSV.")
