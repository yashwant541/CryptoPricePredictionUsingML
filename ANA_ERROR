# Flag 3: one word, alphabets only, only when flag1 and flag2 are false
pattern_one_word_alpha = r"(?i)^[A-Za-z]+$"

df["flag_one_word_alpha"] = (
    ~df["flag_exact_keywords"] &
    ~df["flag_nomination_patterns"] &
    df["text"].str.match(pattern_one_word_alpha, na=False)
)
